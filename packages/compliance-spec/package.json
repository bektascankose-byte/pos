{
  "name": "@snappos/compliance-spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Language neutral conformance fixtures for the compliance rule engine. Run by both the TypeScript evaluator and, when it exists, the Kotlin one on the register.",
  "scripts": {
    "pretest": "npm run build -w @snappos/contracts",
    "test": "node --test test/conformance.test.mjs"
  },
  "dependencies": {
    "@snappos/contracts": "*"
  }
}
